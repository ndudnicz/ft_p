### In progress ...

##### Requirement:
  - nasm
  - replace in libft/libftasm/Makefile:
    "/Users/ndudnicz/.brew/bin/nasm" by your nasm path (`which nasm`)

```shell
make fclean
./configure
make
./serveur [fb] <port>
./client <ip> <port>
``` 
